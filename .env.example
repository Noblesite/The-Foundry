# Optional model registry credentials for gated/private models.
HF_USERNAME=
HF_TOKEN=

# Optional Weights & Biases logging token.
WB_TOKEN=

# Optional local catalog override. Defaults to EMA/runtime/foundry_catalog.db.
FOUNDRY_CATALOG_DB_PATH=
