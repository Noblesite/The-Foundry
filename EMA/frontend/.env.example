VITE_BACKEND_URL=http://127.0.0.1:8000
# Use "mock" for offline UI, "construct-api" for live Construct runtime
# contracts with mock catalog data, or "api" to hydrate everything from FastAPI.
VITE_FOUNDRY_DATA_SOURCE=mock
