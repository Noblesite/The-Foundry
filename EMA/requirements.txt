# Compatibility pointer for older E.M.A. setup commands.
#
# The public Foundry baseline lives at ../requirements.txt.
# This file intentionally installs the optional ML/runtime stack because legacy
# E.M.A. prototype modules imported Transformers, Torch, retrieval, and helper
# integrations directly.

-r ../requirements-ml.txt
