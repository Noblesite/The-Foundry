# Public baseline dependencies for local development.
#
# This installs the API, catalog, ingestion, diagnostics, and contract-test
# surface without pulling local model training/inference packages.
#
# Optional model download, Transformers inference, LoRA training, retrieval,
# and legacy Workspace ONE helper dependencies live in requirements-ml.txt.

-r requirements-app.txt
